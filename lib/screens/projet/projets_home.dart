import 'package:flutter/material.dart';

class ProjetsHome extends StatelessWidget {
  const ProjetsHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Module Gestionnaire de projet (à compléter)",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
