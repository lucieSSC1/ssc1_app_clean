import 'package:flutter/material.dart';

class LoisirsHome extends StatelessWidget {
  const LoisirsHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Module Loisirs (à compléter)",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
