import 'package:flutter/material.dart';

class EtudesHome extends StatelessWidget {
  const EtudesHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Module Études et Emploi (à compléter)",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
